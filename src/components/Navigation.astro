<header class="site-header">
  <nav class="navigation" aria-label="Main navigation">
    <a class="brand" href="/" aria-current="page">Yuta</a>
    <ul>
      <li><a href="/about">About</a></li>
      <li><a href="/links">Links</a></li>
      <!-- <li><a href="/projects">Projects</a></li> -->
      <!-- <li><a href="/blog">Blog</a></li> -->
    </ul>
  </nav>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 10;
    border-bottom: 1px solid rgb(240 246 252 / 8%);
    background: rgb(13 17 23 / 82%);
    backdrop-filter: blur(14px);
  }

  .navigation {
    box-sizing: border-box;
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: min(100% - 3rem, 70rem);
    min-height: 4.75rem;
    margin: 0 auto;
    gap: 2rem;
  }

  .brand {
    color: #f0f6fc;
    font-size: 1.25rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    text-decoration: none;
  }

  ul {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  li a {
    display: block;
    padding: 0.55rem 0.8rem;
    color: #8b949e;
    font-size: 0.95rem;
    font-weight: 500;
    text-decoration: none;
    transition:
      color 160ms ease,
      background-color 160ms ease;
  }

  li a:hover {
    color: #f0f6fc;
    background: rgb(240 246 252 / 8%);
  }

  a:focus-visible {
    outline: 2px solid #58a6ff;
    outline-offset: 3px;
  }

  @media (max-width: 32rem) {
    .navigation {
      width: min(100% - 2rem, 70rem);
      min-height: 4.25rem;
      gap: 1rem;
    }
    ul {
      gap: 0;
      overflow-x: auto;
      scrollbar-width: none;
    }
    ul::-webkit-scrollbar {
      display: none;
    }
    li a {
      padding: 0.5rem 0.6rem;
      font-size: 0.875rem;
      white-space: nowrap;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    li a {
      transition: none;
    }
  }
</style>
